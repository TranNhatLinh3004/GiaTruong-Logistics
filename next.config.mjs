/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.pexels.com", "inkythuatso.com", "xeotogiadinh.com"], // Thêm hostname của ảnh vào đây
  },
};

export default nextConfig;
